---
title: 4.17 TroubleShooting VM
categories: [DAW bilingual, Computer System]
tags: [] # TAG names should always be lowercase
---

## 🔴 TroubleShooting

#### 1️⃣ `⚠️ invalid setting` should be solved

- when you see the sign, solve it

#### 2️⃣ iso change

- only one iso can exist
- so when you want to change, delete the existing one
- and add
- 1️⃣ IDE empty is incorrect ❌
- IDE with `iso vacio` is ok

- 2️⃣ then, you need to go to `general > type, subtype, version`
- and update the `type, subtype, version`

#### 3️⃣ the perfect graphic controller for your `iso` is `VMSVGA`

- but if this creates a `⚠️ invalid setting`
- then change to standard `VboxVGA`

- if asked for add controller with low resolution, add `VGA`

#### 4️⃣ network

- if you add bridge with low number of available IPs
- then change to NAT

## ✅

- unclick unattended install
- click on dynamic RAM
-
